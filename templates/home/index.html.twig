{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Juste une page test pour BD remote Herok ✅</h1>

    <p>List of friends :</p>
    <ul>
        {% for user in users %}
            <li>{{ user.name }}, you are {{ user.age }} and {{ user.height }} cm tall !</li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
